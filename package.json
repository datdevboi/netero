{
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "gen:data": "ts-node-dev --respawn ./scripts/genComponentData.ts",
    "build:netero-ui": "lerna run build --scope=netero-ui && yarn install",
    "build:app": "lerna run build",
    "dev": "nodemon build --watch packages/netero-ui/src/components",
    "test": "lerna run test scope=netero-ui"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**",
      "**/ts-jest",
      "**/ts-jest/**"
    ]
  },
  "devDependencies": {
    "chalk": "^2.4.1",
    "lerna": "^2.11.0",
    "nodemon": "^1.18.3",
    "react-docgen-typescript": "^1.6.3",
    "ts-node-dev": "^1.0.0-pre.26"
  }
}
